/srv/<%= ENV['APP_NAME'] %>/shared/log/*.log {
	missingok
	notifempty
	monthly
	rotate 12
	sharedscripts
	compress
	delaycompress
	postrotate
		systemctl reload nginx.service
		systemctl reload unicorn@<%= ENV['APP_NAME'] %>.service
	endscript
}
